package org.geekhub.encryption.service;

public class EncryptionService {
    public String encryptMessage(String cipherName, String message){
        /*application.addToHistoryLog(message, "Caesar", encryptedMessage);*/
        return "";
    }
}
